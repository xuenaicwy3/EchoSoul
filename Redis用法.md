# 使用一：记录用户最后活跃时间
当用户在聊天界面发送消息时，后端会调用 update_active 方法，将当前时间存入 Redis。 这里 Redis 就是用来记录你最后一次发消息的时间。

# 使用二：存放待推送的主动消息
调度器每隔一段时间（比如 60 分钟）检查一次：有没有用户超过 2 小时没说话？如果有，就生成一句主动问候语，存入 Redis。

# 使用三：前端拉取主动消息
前端定期（或打开页面时）向 /active_messages 接口发起请求，后端从 Redis 中取出并清空该用户的待推送消息。
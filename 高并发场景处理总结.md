负责将原本同步阻塞的单体应用改造成能支撑数百到上千并发的高并发架构。整个改造过程经历了“发现问题 → 全异步尝试 → 多次报错与调试 → 最终收敛为 Celery + Redis Stream 分离架构”的完整路径。

# 一、初始状态与高并发需求分析
项目后端使用 FastAPI 作为 Web 框架，LangGraph 管理对话流程，PostgreSQL 存储聊天记录和好感度，Chroma 作为长期记忆向量数据库，Redis 用于缓存和调度，Celery 负责异步生成回复。

最初系统的核心问题是：
聊天接口同步等待 LLM 生成：/chat 路由直接调用 agent.invoke()，等待 AI 回复完成才返回，导致请求阻塞事件循环，高并发时吞吐量极低。

记忆存储完全失效：代码中 self.memory_svc.store(...) 缺少 await，使得异步存储方法从未真正执行，日志中没有任何记忆写入记录。

记忆检索也失效：Celery Worker 构建 Agent 时没有注入 MemoryService，导致 retrieve 永远返回 “暂无记忆”。

数据库连接池未优化，好感度更新存在并发竞态，主动消息调度扫描全量用户效率低。

因此，高并发改造的目标是：异步化解耦请求与生成，修复并优化记忆存储与检索，保证多轮对话状态在多进程/多 Worker 下的共享与持久化。

# 二、第一次高并发方案：全异步 Redis Stream + asyncio Worker
设计思路
我提出用 Redis Stream 作为消息队列，将请求处理流程变为：
API 层：/chat 接口进行轻量预处理（角色识别、好感度预取），将任务信息打包推入 Redis Stream，立即返回 task_id（平均耗时 < 10ms）。

Worker 层：在 FastAPI 的 lifespan 中启动一个 asyncio 协程，消费 Stream 中的任务，调用 agent.invoke() 生成回复，将结果写入 Redis，并完成聊天记录、好感度、记忆的存储。

前端轮询：前端每隔 0.5 秒请求 /chat/result/{task_id} 获取结果。

状态共享：用 AsyncPostgresSaver 替换默认的MemorySaver内存检查点，让多 Uvicorn worker 共享对话状态。

解释：在 LangGraph 中，对话状态（如角色、情绪、记忆上下文）是通过检查点存储器在多轮对话间保持连续性的。默认的 MemorySaver 把状态放在进程内存里，当使用 Uvicorn 多 Worker 部署时，每个 Worker 进程有独立的内存空间，导致同一个用户的不同请求被分配到不同 Worker 时，无法获取之前的对话上下文。为了解决这个问题，我用 AsyncPostgresSaver 替换了 MemorySaver，把对话状态持久化到 PostgreSQL 数据库中。这样所有 Worker 进程都通过读写同一份数据库记录来获取和保存对话状态，实现了多进程间的状态共享。

# 遇到的核心冲突
1. 异步/同步调用冲突（最严重）
1.1 LLM 调用和 agent.invoke() 是同步方法，Worker 却是异步协程。如果强行把记忆存储、记忆检索的方法改为 async def，在同步调用链中就会产生 RuntimeWarning: coroutine never awaited。

1.2、尝试用 run_in_executor 把同步代码丢到线程池，但又出现 no running event loop 错误。根因：同步与异步边界模糊，试图在异步 Worker 中调用同步 Agent，而记忆服务又在同步和异步之间反复切换。

1.3、RuntimeWarning: coroutine 'MemoryService.store' was never awaited
后来我将 store 改为同步方法，但内部残留了 loop = asyncio.get_running_loop()，被 run_in_executor 放到线程池时，线程中没有事件循环，直接抛出异常。

2. AsyncPostgresSaver 集成失败
2.1、为了多 Worker 共享对话状态，我用了 AsyncPostgresSaver.from_conn_string(self.settings.DATABASE_URL)，结果连续报错：

psycopg.ProgrammingError: invalid connection option "postgresql+asyncpg://..."
psycopg 无法识别 SQLAlchemy 格式的 URL。

ActiveSqlTransaction: CREATE INDEX CONCURRENTLY cannot run inside a transaction block
即使修正 URL 为 postgresql://...?sslmode=disable，setup() 内的建索引操作不能在事务中执行。

2.2 DeadlockDetected
设置 autocommit=True 后，启动两个 Uvicorn worker 时，两个进程同时执行 setup() 创建索引，导致数据库死锁。

尝试了手动建表、自定义 DDL、Redis 分布式锁等，但在 Windows + psycopg 3.x + 多进程环境下始终无法稳定启动。

3. Redis 客户端为 None
Worker 协程启动时，模块级变量 redis_client 还未被 init_redis 赋值，导致 AttributeError: 'NoneType' object has no attribute 'xreadgroup'。后续通过 get_redis_client() 动态获取并判空解决。

# 三、最终收敛方案：Celery 同步生成 + Redis Stream 异步后处理分离
鉴于全异步方案在同步/异步边界和数据库初始化上的不可调和冲突，我调整了架构思想：让同步的归同步，异步的归异步，用 Redis Stream 作为清晰的隔离层。
Celery 同步管理生成过程，FastAPI 异步协程负责后处理，Redis Stream 作为两者之间的解耦层，形成 “同步生成 + 异步存储” 的混合架构。
核心思想是把整个对话流程拆成“生成”和“存储”两个独立阶段，用 Redis Stream 作为中间的缓冲层，让它们各自由最适合的运行时环境处理。

# 核心架构
1. API 层（main.py 的 /chat 接口）
它的职责非常轻：只做参数预处理（角色识别、好感度预取），然后把任务数据打包，调用 Celery 异步任务，立刻返回 task_id。这个接口耗时小于 10 毫秒，完全不阻塞 FastAPI 的异步事件循环。

2. 生成层（tasks.py 的 Celery Worker）
Celery Worker 运行在独立的进程中，使用PostgresSaver 管理 LangGraph 检查点，通过 agent.invoke() 同步生成回复。
两个关键设计：
2.1 agent.invoke() 是同步调用：因为 Celery 任务本身就是同步函数，不存在事件循环，所以完全不会出现 coroutine never awaited 或 no running event loop 的报错。
2.2 生成完后不直接写数据库：而是把后处理需要的数据推入一个单独的 Redis Stream（postprocess_stream），把“生成回复”和“存储记录”彻底解耦。

3. 后处理层（FastAPI 异步协程）：
在 FastAPI 的 lifespan 中启动一个 asyncio 协程，专门消费 postprocess_stream，执行：
这个协程负责所有数据库写入操作：聊天记录存储、好感度更新、长期记忆存储。因为这些操作使用的 asyncpg 是异步驱动，所以在 asyncio 环境中执行是最高效的。
记忆存储（self.memory_svc.store 是同步方法，通过 loop.run_in_executor 放到线程池执行，避免阻塞事件循环）

# 四、这个架构具体解决了哪些问题
问题 1：同步与异步混用导致的报错
这是我们在全异步方案中踩得最深的坑。当时我们把 _memory_node 改成 async def，把 store 也改成 async def，
结果在 Celery 的同步任务中调用时出现 RuntimeWarning: coroutine never awaited；尝试用 run_in_executor 执行异步函数又报 no running event loop。

解决方案：在最终架构中，memory.py 的 store 和 retrieve 全部是纯同步方法（def），内部直接同步调用LLM摘要和嵌入API。
在Celery Worker中，_memory_node直接同步调用retrieve，完全兼容。在后处理协程中，用loop.run_in_executor把同步的 store 丢进线程池执行，不阻塞事件循环。
这样彻底划清了同步与异步的边界：Celery 永远只调用同步代码，FastAPI 协程中需要同步操作时就丢进线程池。

问题 2：AsyncPostgresSaver 在多 Worker 下初始化死锁
在全异步方案中，我们试图在 Uvicorn 的 lifespan 中用 AsyncPostgresSaver.from_conn_string() 初始化检查点，
结果连续遇到 URL 格式不兼容、CREATE INDEX CONCURRENTLY 事务冲突、两个 Worker 同时建表死锁三个错误。

解决方案：在最终架构中，检查点的初始化完全移到了 Celery Worker 中。在 tasks.py 的 _build_agent() 里，
使用同步的 PostgresSaver，每个 Worker 进程创建自己的同步数据库连接：
因为 Celery Worker 通常只启动一个进程（或者多个独立进程），不存在多个进程同时执行 setup() 建表的情况，
所以彻底避免了死锁。Uvicorn 的多 Worker 只负责 API 请求，完全不接触检查点。

问题 3：好感度并发更新丢失
原来的好感度更新是“先读后写”：先 get 当前值，加上增量，再 insert on conflict update。高并发下两个请求同时读到旧值，后写的一个会覆盖前一个的更新。
解决方案：改为数据库原子操作，不先读数据，直接在 SQL 层面做加法：用 LEAST 和 GREATEST 限制范围 0~100，一行 SQL 完成加减和边界控制，彻底消除竞态。

问题 4：记忆存储和检索方法完全没有被调用
1. 先查记忆存储为什么没写进去
记忆存储是在 agent.py 的 finalize_conversation 方法里触发的，这个方法在 Celery Worker 生成回复后，由后处理协程调用。
原来finalize_conversation方法中调用memory_svc.store方法没有加await,而memory.py 里的 store 方法是 async def。
这里少了 await，导致 Python 只是创建了一个协程对象，但从来没有真正执行它。所以记忆存储实际上从未发生。修复很简单，补上 await 就行。
但问题来了：这个后处理协程是 asyncio 环境，如果直接 await 一个包含同步 LLM 调用的异步函数，会阻塞事件循环。
所以我必须把 store 改成纯同步方法，然后在调用端用 run_in_executor 丢到线程池：
同时把 memory.py 里的 store 和 retrieve 全部改成 def，不再用 async/await。这样 Celery 的同步环境直接调用没问题，异步环境用线程池执行也不阻塞。


2. 再查记忆检索为什么返回空
记忆检索是在 Celery Worker 的 process_chat 任务里，通过 agent.invoke() 执行的 _memory_node 节点触发的。
我打开 tasks.py 的 _build_agent()，发现当时创建 Agent 时根本没有传入 MemoryService。
而 agent.py 的 __init__ 在 memory_svc=None 时会创建一个空壳方法，retrieve 永远返回“暂无记忆”。所以检索一直是空的。
修复就是在 _build_agent() 中实例化 MemoryService 并传入。


# 五、总结
这个高并发改造项目的核心经验是：在 Python 生态中，高并发架构不一定要追求全部异步化。当第三方库或遗留代码本身是同步的，强行统一异步往往会引发大量隐式冲突。 最终我们选择让 Celery 同步管理生成过程，FastAPI 异步协程负责后处理，Redis Stream 作为两者之间的解耦层，形成 “同步生成 + 异步存储” 的混合架构。这个方案既稳定又易于扩展，是一个务实的技术决策。

整个调试过程中，我们从异步混用、数据库初始化死锁等具体问题中逐步收敛，最终沉淀出一套可以支撑生产环境高并发的成熟方案。如果未来需要更高的吞吐，只需横向扩展 Celery Worker 数量，并考虑前端轮询升级为 WebSocket 即可。
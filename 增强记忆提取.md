# 问题：记忆检索时使用的 user_id 与之前存储记忆时的 user_id 不一致，导致无法匹配到任何记录，因此总是返回“暂无记忆”。
前端每次生成或刷新页面时，user_id 都发生了变化

# 解决方案
# 0. 从 localStorage 获取或创建持久化的 user_id
let USER_ID = localStorage.getItem('echo_user_id');
if (!USER_ID) {
    USER_ID = "web_user_" + Math.random().toString(36).substr(2, 8);
    localStorage.setItem('echo_user_id', USER_ID);
}
console.log('当前用户ID:', USER_ID);

# 1. 增强记忆提取（让模型更“贪婪”）
修改 app/prompts.py 中的 memory_extraction 函数，添加更明确的引导，要求模型即使通过提问也能推断出用户兴趣，并降低返回“无”的概率。
@staticmethod
def memory_extraction(user_msg: str, ai_reply: str) -> str:
    return (
        "请从下方对话中提取关于用户的事实、偏好、情绪、计划或兴趣，"
        "包括从提问中可合理推断的信息。例如，用户询问某部作品，表示对其感兴趣。"
        "用第三人称概括，每条不超过20字，用换行分隔。\n"
        "示例：\n"
        "用户：你看过《鬼灭之刃》吗？\nAI：当然！\n关键信息：用户对《鬼灭之刃》感兴趣\n"
        "如果确实没有任何信息，才输出“无”。\n"
        f"对话：\n用户：{user_msg}\nAI：{ai_reply}\n关键信息："
    )

# 2. 更换更强大的记忆提取模型
在 app/memory.py 中将 summary_llm 改为 qwen3.6-plus，并降低温度以提高稳定性

# 3. 让 AI 在回复时更主动地参考记忆
在 app/prompts.py 的 system_prompt 中增加一句话，明确要求 AI 提及记忆中已知的用户偏好：
@staticmethod
def system_prompt(role_type, emotion_style, memory_text, aff_info="", unlock_info=""):
    role = RoleCatalog.get_role(role_type)
    prompt = f"""你叫{role.name}。……（前面的内容不变）
【关于用户的记忆】{memory_text}
请根据记忆中的信息，在对话中自然地提及或回应用户的已知偏好，让对话更贴心。
……"""
    return prompt

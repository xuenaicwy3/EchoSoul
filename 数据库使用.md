# PostgreSQL 数据库使用

1、以超级用户（postgres）登录 psql
psql -U postgres

2、重置 echosoul 的密码
-- 创建用户 echosoul
CREATE USER echosoul WITH PASSWORD '123456';

-- 创建数据库 echosoul，并设置所有者为 echosoul
CREATE DATABASE echosoul OWNER echosoul;

-- 授予所有权限
GRANT ALL PRIVILEGES ON DATABASE echosoul TO echosoul;

ALTER USER echosoul WITH PASSWORD '******';
然后退出：\q

3、验证连接
psql -h localhost -p 5432 -U echosoul -d echosoul

4、重启服务
Restart-Service postgresql-x64-18

5、强制重新创建数据库表
psql -U echosoul -d echosoul
DROP TABLE IF EXISTS chat_history, affection;
\q

# 查看数据库数据
1、使用 psql 命令行
psql -U echosoul -d echosoul
SELECT * FROM chat_history ORDER BY timestamp DESC LIMIT 10;

2、查看数据分布
SELECT tableoid::regclass AS partition, count(*)
FROM chat_history
GROUP BY tableoid
ORDER BY partition;

更通俗的理解方式
tableoid → 每行数据来自哪个物理子表（用数字标识）
::regclass → 把那个数字翻译成表名
GROUP BY tableoid → 把同一个物理子表的数据放在一起
count(*) → 数一数每个物理子表里有多少行





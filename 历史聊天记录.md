# 本地存储

# 一、核心实现原理
我们使用了 Chroma 向量数据库 来存储聊天记录。虽然名字叫"向量数据库"，但我们把它的向量功能禁用掉，只当作文档存储来用。每条消息都是一个"文档"，附带丰富的元数据。

数据模型

每条聊天消息存储时包含以下信息：

python
{
    "消息内容": "你看过学战都市吗",
    "元数据": {
        "user_id": "web_user_nscs8xai",    # 哪个用户
        "role_type": "日系动漫型",          # 哪个角色
        "sender": "user",                   # 谁发的
        "timestamp": "2026-05-31T21:52:42"  # 发送时间
    }
}

# 二、存储实现（写入数据）

1. 后端接收消息时存储

在 app/main.py 的 chat 方法中，每次对话结束后：

存储用户消息

self.chat_history.add_message(
    user_id=req.user_id,      # "web_user_nscs8xai"
    role_type=final_role,     # "日系动漫型"
    sender="user",            # 发送者是用户
    message=req.message       # "你看过学战都市吗"
)

存储 AI 回复

self.chat_history.add_message(
    user_id=req.user_id,      # "web_user_nscs8xai"
    role_type=final_role,     # "日系动漫型"
    sender="ai",              # 发送者是AI
    message=final["final_response"]  # AI的回复内容
)

# 2. ChatHistoryManager 内部实现
在 app/chat_history.py 中，add_message 方法
def add_message(self, user_id: str, role_type: str, sender: str, message: str):
    # 生成唯一ID，避免重复
    doc_id = f"{user_id}:{role_type}:{sender}:{hash(message)}"
    
    # 存入 Chroma
    self.store.add_texts(
        texts=[message],           # 消息内容
        metadatas=[{               # 关键元数据
            "user_id": user_id,    # ← 查询过滤条件
            "role_type": role_type, # ← 查询过滤条件
            "sender": sender,
            "timestamp": datetime.now().isoformat()
        }],
        ids=[doc_id]
    )
    # 立即持久化到磁盘
    self.store.persist()

# 三、查询实现（读取数据）
1. 前端请求历史

当用户切换到某个角色时，前端自动请求

// script.js 中的 loadHistory 函数
async function loadHistory(roleType) {
    // 发送 GET 请求到后端
    const res = await fetch(`/chat_history/${USER_ID}/${encodeURIComponent(roleType)}`);
    const data = await res.json();
    // 将历史消息渲染到聊天区
    data.history.forEach(msg => {
        addMessage(msg.sender, msg.message, msg.timestamp);
    });
}

2. 后端返回历史

在 app/chat_history.py 中，get_history 方法：
def get_history(self, user_id: str, role_type: str) -> List[Dict]:
    # 获取底层 Chroma 集合
    collection = self.store._collection
    
    # 核心查询条件：必须同时匹配 user_id 和 role_type
    results = collection.get(
        where={
            "$and": [
                {"user_id": user_id},     # ← 必须是这个用户
                {"role_type": role_type}  # ← 必须是这个角色
            ]
        },
        include=["documents", "metadatas"]
    )
    
    # 组装结果
    messages = []
    for doc, meta in zip(results["documents"], results["metadatas"]):
        messages.append({
            "sender": meta["sender"],      # user 或 ai
            "message": doc,                # 消息内容
            "timestamp": meta["timestamp"]  # 发送时间
        })
    
    # 按时间排序
    messages.sort(key=lambda x: x["timestamp"])
    return messages

# 四、隔离机制图解
用户 A + 角色小樱 (日系动漫型)
├── 消息1: 用户: "你看过学战都市吗"
├── 消息2: AI: "当然看过啦！超燃的！"
└── 消息3: 用户: "大结局是什么"

用户 A + 角色雪乃 (高冷御姐型)
├── 消息1: AI: "……来了啊。有什么想说的，我听着。"
├── 消息2: 用户: "看过我的青春恋爱物语吗"
└── 消息3: AI: "自然是看过的。"

隔离的关键：查询时同时使用 user_id + role_type 两个条件，确保每个角色的聊天记录完全独立。

# 五、聊天记录隔离
聊天记录存储在 Chroma 向量数据库的 chat_history 集合中，每条消息的 metadata 里都包含了 user_id 和 role_type 字段。查询时，系统会使用这两个字段组合进行精确过滤，从而确保每个角色的聊天记录完全独立，实现了彻底的隔离。

存储示例：

用户ID: web_user_123
角色: 日系动漫型
发送者: user
消息: 你看过学战都市吗

查询逻辑：

where={"$and": [{"user_id": user_id}, {"role_type": role_type}]}

记忆系统：

记忆系统利用阿里云的 text-embedding-v4 模型将对话摘要向量化，并存储在 Chroma 的 companion_memories 集合中。检索时，通过语义相似度找出与当前话题最相关的记忆，为 AI 提供上下文。



# PostgreSQL数据库存储
查看数据分布
SELECT tableoid::regclass AS partition, count(*)
FROM chat_history
GROUP BY tableoid
ORDER BY partition;

1. FROM chat_history
chat_history 是逻辑主表（分区父表）。虽然实际数据存储在多个物理子表（如 chat_history_傲娇辣妹型）中，但你查询这张主表时，PostgreSQL 会自动扫描所有分区并合并结果，就像查询一张普通表一样。

2. tableoid
这是 PostgreSQL 为每一行数据自动添加的隐藏列，存储该行实际所在的物理表的 OID（对象标识符）。OID 是一个数字，例如 24576，代表某个具体的物理表。

text
逻辑主表：chat_history
├── 物理子表：chat_history_傲娇辣妹型 (OID: 24576)
├── 物理子表：chat_history_日系动漫型 (OID: 24577)
└── ...
当你查询 chat_history 时，每一行数据其实都来自不同的物理子表，tableoid 会告诉你这一行真正属于哪个子表。

3. ::regclass
这是 PostgreSQL 的类型转换语法。它将一个代表 OID 的数字转换成人类可读的表名。

sql
-- 例如：
SELECT 24576::regclass;  -- 可能返回 chat_history_傲娇辣妹型
因为原始的 OID 是一个毫无意义的数字（如 24576），通过 ::regclass 转换后，你就能直接看到对应的表名了。

4. AS partition
给转换后的列取一个别名，叫 partition，方便阅读结果。

5. count(*)
统计每个分区内的行数。

6. GROUP BY tableoid
按物理表的 OID 进行分组。同一物理子表的所有行拥有相同的 tableoid，因此每个角色分区的数据会聚合到一组。

7. ORDER BY partition
按分区表名排序，让输出更整齐。

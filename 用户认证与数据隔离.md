# 一、整体认证与隔离流程
[注册] → [登录] → 返回 JWT → [后续请求携带 JWT] → [中间件解析 JWT 得到 user_id] → 
[数据库查询强制带上 user_id] → 返回该用户自己的数据

# 二、注册逻辑
1、客户端提交 username、password。
2、后端检查用户名是否已存在于 PostgreSQL users 表。
3、密码不存明文，使用 bcrypt 加盐哈希（hash_password）。
4、插入新记录，返回成功。
笔记：密码哈希确保即使数据库泄露，也无法逆推出原始密码。

# 三、登录与 JWT 生成
1、客户端提交 username、password。
2、后端根据用户名查出用户，用 verify_password 比对哈希。
3、验证通过后，使用jwt.encode方法生成JWT：
    payload 包含：{sub :user_id（字符串形式）, username、jti（唯一标识）、exp（过期时间戳）}
    签名：服务端用 SECRET_KEY 进行 HMAC-SHA256 签名。
4、将 JWT 通过 Bearer Token 形式返回给客户端。
笔记：JWT 是无状态的，服务器不保存会话，所有信息都在 token 里

# 四、 后续请求的认证（中间件/依赖项）认证依赖项 [Depends(get_current_user)]
1、客户端携带 JWT：客户端在每次请求的 HTTP 头中加入： Authorization: Bearer <JWT>
2、后端提取并验证 Token：从请求头中提取 token, 用 SECRET_KEY  验证 JWT 签名和有效期。
3、解析出用户标识：调用 jwt.decode() 得到 payload，从 payload 中取出 sub 字段。
4、返回用户标识，如果验证失败（无 token、签名错误、过期），则抛出 401 Unauthorized，请求不会进入业务接口。

# 五、业务接口中的强制数据隔离（核心） 
1、业务接口（如 /chat）通过依赖注入拿到 current_user 后，在所有数据存储和状态管理中，必须使用这个 current_user 来区分用户，不能接受客户端传来的任何用户 ID。
2、会话状态隔离：在 /chat 接口中，构造 LangGraph 的 thread_id 时
    thread_id = f"{current_user}:{final_role}" if final_role else current_user
    config = {"configurable": {"thread_id": thread_id}}
current_user 来自 JWT，确保了不同用户的对话历史（checkpoint）被天然隔离在各自的 thread_id 下。

3、聊天历史持久化（chat_history表）
在 finalize_conversation 中：直接传入 JWT 解析出的用户标识，其内部会向 chat_history 表插入一条记录，其中 user_id 字段的值就是 current_user。
客户端无法伪造这个值，因为它来自 JWT。

